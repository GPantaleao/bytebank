const Sidebar = () => {
  return (
    <aside className="w-[432px] bg-primary-600 min-h-screen flex flex-col items-center py-10">
      {/* Logo ou Avatar podem ir aqui futuramente */}
      <nav className="w-full">
        {/* Links de navegação */}
      </nav>
    </aside>
  );
};

export default Sidebar;
